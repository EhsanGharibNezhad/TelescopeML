language: python
python:
  - "3.9"

install:
  - pip install TelescopeML

script:
  - python setup install

branches:
  only:
    - main  # Adjust to your main branch name, e.g., master

notifications:
  email: false

